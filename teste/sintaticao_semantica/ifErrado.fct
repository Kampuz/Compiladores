program ifCorreto;

   int  a, b, c ;       //Declaracao de variaveis. E necessario utilizar a  Reservada_Var.
          int d, e, f;      //Pode ser declarada uma lista de variaveis.
          boolean g;     //Ou uma so variavel
  begin
     if (a > 0) and (b > 0)             //If Errado falta a palavra then
        write (a);
     else
        write (d);

     if (a > 0) then                      //if errado, dois comandos sem utilizar a begin/end
       d := e*3;
       e := f div 4 + d div 2
    else
       c := 3;

       
  end.